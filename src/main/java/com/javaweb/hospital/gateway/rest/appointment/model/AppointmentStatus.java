package com.javaweb.hospital.gateway.rest.appointment.model;

// Define the possible appointment statuses based on your appointment service
// Example:
public enum AppointmentStatus {
    WAITING,
    CONFIRMED,
    REJECTED,
    COMPLETED,
    CANCELED,
    EXPIRED
}

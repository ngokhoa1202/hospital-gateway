package com.javaweb.hospital.gateway.rest.appointment.model;

public enum AppointmentType {
    RE_EXAMINATION, // Tái khám
    SURGERY, // Phẫu thuật
    GET_RESULTS, // Lấy kết quả
    EXAMINATION, // Khám
    GET_TEST_RESULTS // Lấy xét nghiệm
}
